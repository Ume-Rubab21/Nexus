export type Student = {
  _id?: string;          // optional for initial state
  studentID?: string;    // optional, visible ID
  name?: string;
  email?: string;
  bio?: string;
  department?: string;
  contactInfo?: string;
};
